<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>{{fontName}}</title>
	<style>
		body {
			font-family: sans-serif;
			margin: 0;
			padding: 10px 20px;
		}

		.preview {
			line-height: 2em;
		}

		.preview__icon {
			display: inline-block;
			width: 32px;
			text-align: center;
		}

		.icon {
			display: inline-block;
			font-size: 16px;
		}

		{{{styles}}}
	</style>
</head>
<body>
	<h1>{{fontName}}</h1>
	{{#each namesandcodes}}
	<div class="preview">
		<span class="preview__icon">
			<span class="{{../baseClass}} {{../classPrefix}}{{name}}"></span>
		</span>
		<span>{{name}}: {{{code}}}</span>
	</div>
	{{/each}}
</body>
</html>
